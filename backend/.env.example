# Copy to .env in this folder for local development (dotenv-rails loads it automatically).
# Generate a secret: ruby -e "puts SecureRandom.hex(64)"
SECRET_KEY_BASE=

# Postgres (defaults match typical local Postgres installers)
DATABASE_HOST=127.0.0.1
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
